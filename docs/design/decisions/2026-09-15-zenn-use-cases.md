# 悩み別の記事をメイン記事から案内する

- 決定日時：2026-09-15（JST）
- 決定担当：Codex、この会話の執筆担当（personal profile）
- 根拠：ユーザーの「メイン記事から切り口ごとにリンク」「その問題はこうだよ」「repoの著作物として」という指示。

## 決定と理由

原稿を `publications/zenn-use-cases-v0/` に置く。既存の `publications/zenn-v0/` は章順に読む本であり、今回は困りごとから読む独立記事シリーズなので別に管理する。

メイン記事は、具体的な悩み・doeffでの短い解き方・詳細へのリンクで構成する。機能を列挙するだけでは自分の問題との関係が見えず、全説明を一記事へ入れると入口が重くなるため。

原稿の正本はリポジトリ内のMarkdownとし、プレビューはそこから生成する。公開はまだ行わない。

## 戻す手順

記事の分割を見直す場合は各原稿を統合し、メイン記事と `publications/README.md` の案内を更新する。追加したシリーズを取りやめる場合は、本変更の対象だけをgitで戻す。既存のZenn本や他者の変更には触れない。

## 冒頭を短いコードから始める（同日追記）

- 決定担当：Codex、この会話の執筆担当。
- 根拠：ユーザーの「早めに短いコード」「フレームワークのLPみたいに」という指示。
- 採用：一言の価値、5行の処理本体、実時間と仮想時間の違い、詳細リンクの順で導入する。完全な実行コードは直後の折りたたみに置く。
- 理由：抽象的な説明を読む前に、実際の書き方と得られる動作を見せるため。
- 戻す場合：冒頭の導入と折りたたみを編集する。個別記事の構成とリンクはそのまま維持できる。

## packages全件とゲーム進行を反映（同日追記）

- 決定担当：Codex、この会話の執筆担当。
- 根拠：ユーザーの「packagesを見てちゃんと網羅」「ゲームエンジン」「カードゲームのロジックやイベント待ちループ」という指示。
- 採用：全31ディレクトリを編集用網羅表へ対応付け、メイン記事から16本の個別記事へ案内する。廃止済み・検証用・開発途中のパッケージも除外理由や確認範囲を明示する。
- 理由：5つの基礎機能だけではエコシステムの用途を過小に紹介していた。利用者には悩み別、編集者にはパッケージ別という2つの経路が必要なため。
- 戻す場合：個別記事を統合し、メイン記事と網羅表のリンクを更新する。原稿の正本は引き続きpublications配下とする。

## doeff-agentsを最初の入口にする（同日追記）

- 決定担当：Codex、この会話の執筆担当。
- 根拠：ユーザーの「AIが流行っているのでdoeff-agentsも推して行きたい」という指示。
- 採用：LPの冒頭を短いエージェント起動・結果待ちの定義にする。AIの案内を先に置き、単発LLMとエージェントの違い、結果スキーマ、他の計算との合成を個別記事で説明する。
- 理由：具体的な入口で関心を持ってもらい、共通の計算基盤という主題と全用途の案内へつなげるため。
- 戻す場合：冒頭コードを時間記事の例へ変更し、案内の順を戻す。時間の実行例自体は個別記事に残っている。

## 解釈の自由を中心にし、概念図と処理図を付ける（2026-09-15追記）

- 決定担当：Codex、この会話の執筆担当。
- 根拠：ユーザーの「effectをどう解釈するかはhandler次第」「別のフレームワークに差し替え」「それぞれの章で概念を説明するキービジュアル」「処理の様子を示したダイアグラム」という指示。
- 採用：メイン記事は短いエージェント例を維持し、その直後から、標準・独自・テストのハンドラへ差し替える価値を説明する。多段メモ化・ドメイン境界・Hyを独立記事に追加し、20記事に概念図・処理図を1枚ずつ付ける。yieldの歴史は関数の色の記事に統合する。
- 理由：標準ハンドラの機能一覧だけでは拡張可能性が伝わらない。読者が操作の境界と処理の流れを見分けられるよう、概念図は暗色、処理図は明色にする。横長すぎる図は縦配置にする。
- 画像の正本：publications配下のDOT・SVGと、Zennの仕様に合わせたルートimages配下のPNG。生成スクリプトも同じシリーズに置く。画像生成の有償APIは使わない。
- 戻す場合：Markdownの画像参照と該当図だけを除く。記事の統合時はメイン・README・網羅表のリンクを更新する。他のシリーズや他者の変更は戻さない。
- 検証：3層メモ化、ドメイン操作、Hyの例を実行。記事リンク・構文・図の容量・表示を確認する。外部サービスの実行と公開は含めない。

## 全指示を追跡し、実用例と検証を揃える（2026-09-15）

- 決定担当：Codex、この会話の執筆担当。
- 採用：TODOを指示の正本、package-coverageをパッケージの棚卸し、feature-examplesを機能・コード・確認範囲の対応表とする。READMEからすべてへ案内する。
- 実用例：非公開実装からは操作の境界・保存階層・時間と並行処理の合成という概念だけを参照し、文書と架空のゲームで独立したコードを著作する。元のコード・名称・接続先・運用条件は掲載しない。
- 検証：外部APIや有料処理を実行せず、オフラインの例を明示的に選んで実行する。外部接続の構成例・CLI未実行・既知のimport問題を、成功した機能と区別する。
- 画像：global gitignoreでPNG/SVGが除外されていたため、本シリーズの画像パスだけ例外にする。原稿と一緒に管理できるようにするため。
- 閲覧：HTMLに画像とコードのダウンロードを埋め込む。原稿はリポジトリに保持し、/tmpは生成プレビューと検証の一時出力にだけ使う。
- 戻す場合：本シリーズの原稿・図・検証スクリプト、.gitignoreの2つの例外と案内リンクだけを戻す。実行基盤や他の作業の変更には触れない。

## imagegenで全章の画像を制作する（2026-09-15追記）

- 決定担当：Codex、この会話の執筆担当。
- 根拠：ユーザーが「imagegen skill to create key visuals and diagram」と明示。旧Graphviz版は画像制作要求の完了条件を満たしていなかった。
- 採用：組み込みimagegenで概念図20枚・処理図20枚を生成し、必要な局所編集も同ツールで行う。PNGはimages配下のgeneratedディレクトリ、プロンプトと採用台帳はシリーズ配下のimagegenディレクトリへ保存する。旧版の図は比較資料として残す。
- 理由：ユーザーが求めた表現と制作方法を満たし、各章の概念と処理の違いを説明するため。概念図の背景を一律に暗色とする従前の方針は解除する。
- 戻す場合：本文40箇所の画像参照からgenerated/を外して旧版へ戻す。画像台帳・制作記録と検査対象も合わせて戻す。他のシリーズや実行基盤には変更を加えない。

## 平面的な図解とコード例を制作基準にする（2026-09-16）

- 決定担当：Codex、この会話の執筆担当。
- 根拠：ユーザーの「keep using this tone」「we prefer diagram, but with code example」と2枚の比較指定。
- 採用：白背景、平面的な図形と矢印、短いコード例を使う。立体的な装置のイラストという従来の概念図の方針を変更する。指摘された開発支援図を差し替え、以後の制作へ適用する。
- 理由：見た目の装飾より、概念と具体的な書き方の対応を読者へ示すため。
- 戻す場合：開発支援図は旧画像を保持し、PNGと画像台帳の採用情報を旧版へ戻す。制作基準の追記も対応して戻す。

## メイン記事を「doeffとは？」の入口にする（2026-09-16）

- 決定担当：Codex、この会話の執筆担当。
- 根拠：ユーザーの「the main page should be more like 'what is doeff?'」。
- 採用：定義、短いAskの例、エフェクト・ハンドラ・Programの説明、実行例、用途別の導線の順にする。Agentsは用途紹介の先頭に保持する。
- 理由：特定用途の前提知識なしで、doeffの仕組みと使う意味を理解できる入口にするため。
- 戻す場合：冒頭を以前のエージェント例へ戻し、概念図と制作台帳も旧版へ戻す。個別記事と用途リンクは維持できる。

## 記事別の独立精査と画像の全面更新（2026-09-16）

- 決定担当：Codex、この会話の執筆担当。
- 根拠：ユーザーの「記事ごとにこの基準で精査するエージェントを立てて修正して、画像の差し替えも進めて」。合成関数をasync defで書きAwaitへ包む例が誤りと指摘された。
- 採用：22記事を個別の担当エージェントが確認する。呼び出し規約・実装との一致・@doによる合成を確認し、指摘と検証をreviewsへ保存。画像44枚は白背景・コード付き図解へ更新する。Rust VMとハンドラ合成の記事も完成対象に含める。
- 理由：モックで動作することだけでは正しい用法や説明を保証できず、異なる担当による実装照合が必要なため。
- 戻す場合：原稿・例・画像・制作記録の本作業分を戻す。旧PNGはarchive配下に保持。ランタイム製品コードは変更対象外。

## 最新の完成範囲とHTTPの責務分離（2026-09-16）

- 決定担当：この会話のCodex（personal profile）。
- 採用：coroutine比較を含む23記事へ拡張し、記事別エージェントが実装を照合する。全コード例の実質行に目的と期待結果のコメントを付ける。全46画像をコード付きの平面図へ差し替える。
- HTTP：利用側でclient_factoryを注入する例は、handler交換の価値と合わない。HttpRequestを処理するハンドラと、Memo書き換え・保存先のハンドラを分ける。文書ドメインの翻訳と時計も独立に合成する。
- 理由：ユーザーの呼び出し方・各行コメント・coroutine比較・HTTP設計への指摘を満たし、実行結果だけでなく責務分離を示すため。
- 制限：製品に残る旧Pass/Delegate、flowのimport、ml-nexusの契約不整合は記事に明記し、実装成功を誇張しない。製品コードの修正はこの原稿制作へ含めない。
- 戻す手順：本シリーズの本文・例・画像と採用台帳を対応する版へ戻す。旧図はarchiveに保持。製品コード・他者の変更を戻さない。

## 最小ハンドラを冒頭で定義し、悩みの導線を表へ揃える（2026-09-16）

- 決定担当：この会話のCodex。
- 根拠：ユーザーの「effectとhandlerが必須」「ミニマムなhandlerも見せないと意味不明」「悩みと解法はテーブル」「@handler @do def handle_ask_effect(effect,k)」。
- 採用：冒頭でAskをyieldするgreetと、@handler・@do付きのhandle_ask_effectを同じコードに掲載する。Resume(k, 値)で続きを再開し、Pass(effect, k)で担当外を渡すことまで示す。設置はwith_handlersで明示し、標準ハンドラの呼び出しだけを見せる例を置き換える。
- 構成：全22個別記事の悩み・解法・リンクを用途別の表へ統一する。メインの概念図・処理図も独自ハンドラの定義と同じコードへ更新する。
- 理由：処理本体だけでは、エフェクトの受け手と再開の方法が見えない。短さを理由にこの対応を省略しない。利用者が悩みから記事を比較して選べるようにする。
- 戻す手順：冒頭・表・main図2枚と対応する仕様・画像台帳・検証記録を同時に戻す。旧画像をarchiveへ保持し、製品コードと他記事の変更は戻さない。

## メイン最上部のキービジュアル（2026-09-16）

- 決定担当：この会話のCodex。
- 根拠：ユーザーの「we need an eye catching image at the top of main article」。
- 採用：記事本文の最上部、定義の前に横長のキービジュアルを1枚追加する。大きいdoeffの文字と、日本語の短い価値説明、AskとResumeの往復、用途のチップで構成する。既存の概念図・処理図46枚と合わせて47枚とする。
- 理由：冒頭で関心を引き、処理と実行方法を分ける主題をひと目で示すため。最新の配置指示に合わせ、従来の「画像より前にコード」の検査を「冒頭画像の直後に定義と最小コード、その後に詳しい図」へ更新する。
- 制作：組み込みimagegenを使い、白背景・平面図・日本語・短いコードを維持する。本文の最小ハンドラと悩みの表は維持する。
- 戻す手順：main-heroのMarkdown参照・PNG・採用台帳・検査の追加を対応させて戻す。既存46画像と原稿のコードは変更しない。

## 最初の7日間を毎日1本の連載にする（2026-09-16）

- 決定担当：ユーザーの承認を受け、この会話のCodexが記録した。
- 根拠：7日間の題材・順序と、メインを目次として更新する提案に対して、ユーザーが「sounds perfect」と回答した。
- 採用：メイン、Agents、仮想時間、HTTPとMemo、カードゲーム、coroutine比較、DIの順で1日1本。各回は単独で理解できる内容にし、メインには公開済み記事のリンクを順次追加する。第2週以降は質問を見て順序と間隔を調整する。
- 理由：読者ごとの具体的な悩みを入口にし、使い方からdoeffの全体像へつなげるため。
- 記録：publications/zenn-use-cases-v0/publication-plan.mdに原稿との対応、各回の仕上げ基準、公開状態とリンクの更新手順を保存する。開始日・時刻と公開先の設定は未実施。
- 戻す手順：公開前の順序・間隔は公開計画と本記録を更新する。公開済みのURLは維持し、メインの案内を新しい順序へ合わせる。原稿・図・公開済み記事は削除しない。
