# 事前の主張の結果(盲検の後・2026-09-22)

`claims.md` は盲検 A・B の起動より前に固定したもので、起動後は 1 文字も直していない。
ここはその主張が盲検と反例の行列でどうなったかの事後の記録。

| # | 主張(事前) | 結果 | 何が起きたか |
| --- | --- | --- | --- |
| 1 | 同じ頂点の form の中で組み替える実装は、綴りに依らず赤 | **反証された → 設計を直した** | 盲検 B: 束ねた名ではなく、名簿の読み手が積んだ先 `bodies` を後から歩いて見出しを書き足す形が緑で通過(`blind-B.md`)。試作 2 版で「運ぶ名の閉包」まで読む形にし、B の形は赤(`numbered`) |
| 2 | 読み手を足す/替える変更は、名簿への 1 行の宣言で緑・宣言しなければ赤 | 成立 | `newreader`(赤)/ `newreaderdeclared`(緑)、盲検 A の `lencheck`(赤)/ `lencheckdeclared`(緑)の 2 対 |
| 3 | 意味を変えない整理(折り直し・束ねた名の改名・欄の順・呼びに引数を足す)は緑のまま | 成立(列挙した 4 形は緑)・**ただし範囲の外に「正当でも赤になる」形が 2 つ在ることが分かった** | 盲検 A: 組み替えない読み `(len text)` は赤。当席: 積む先の名 `bodies` の改名は名簿の鍵 `"bodies.append"` と食い違って赤(`renamecarrier`)。どちらも検査を緩めず、註に「過剰に赤くする範囲」として名指し、直し方 = 名簿を人が 1 行直す(`renamecarrierdeclared` = 緑) |

## 変わると予測した範囲(事前)との照合

- 冊 1 file のみ — **成立**(`git diff --name-only` = 冊 1 本)。
- `:statement` は 1 行も変えない — **成立**(差分 0 行)。
- 実コードは 1 byte も変えない — **成立**(`sessionhost/**` の差分 空)。

## 覆わないと最初から宣言した範囲との照合

B の形は宣言した 3 つ(別の頂点の form・handler の実 I/O・呼ばずに作る形)の**どれにも当たらない**
= 宣言の外の穴だった。⇒ 「覆わない範囲」の宣言は正しかったが「覆う範囲」の実装が足りなかった。
試作 2 版の註は、赤にする範囲・赤にしない範囲・過剰に赤くする範囲の 3 つを名指しで並べる。
