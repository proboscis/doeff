# 事前に固定した主張(反例の生成より前・2026-09-22 JST)

登記の場所 = `blind-input.md` §7(sha256 `7ca29cbab4d2f26d9fc0f351a27ae3699cf4b6ab4812b8d958d08491ac4c585c`)。
盲検 A・B の起動より前に書き、起動後は 1 文字も直していない。

| # | 主張 | 前提 |
| --- | --- | --- |
| 1 | `mail-turn-text-of` が返した文を**同じ頂点の form の中で**組み替える実装は、書き方の綴りに依らず赤になる(行の折れ方・空白・局所変数の名・どの構文で束ね直すかに依らない) | 覆うのは同じ頂点の form の中ちょうど |
| 2 | 文の読み手を足す/替える変更は、冊の名簿への 1 行の宣言で緑になり、宣言しなければ赤になる | 名簿はこの冊の唯一の宣言点 |
| 3 | 意味を変えない整理(折り直し・束ねた名の改名・欄の順の入れ替え・呼びに引数を足す)は緑のまま | — |

## 変わると予測した範囲(事前)

- 冊 `docs/adr/defadr_doeff_agents_012_agentd_acp_arms.hy` 1 file のみ。
- `:statement`(法の条文)は 1 行も変えない。
- 実コード(`judgment.hy` / `agentd.hy` / `effects.py` / `handlers.py`)は 1 byte も変えない。

## 覆わないと最初から宣言した範囲

- 別の頂点の form へ渡った後の組み替え。
- `handlers.py` の実 I/O の中での組み替え。
- `mail-turn-text-of` を呼ばずに文を作る形。
