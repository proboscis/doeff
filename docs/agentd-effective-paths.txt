# agentd に効く path の名簿
#
# 「この repo の着地が、走っている agentd の中身を変えるか」の**定義点はこの file 1 つ**。
# 何が agentd に効くかを知っているのは doeff なので、名簿の持ち主も doeff。読み手は
# 値を読むだけで、判定の code は repo をまたいで共有しない。
#
# ── 誰が読むか(2026-09-19 現在)
#   * agent-control-plane の配備の拍 `deploy/acp-control/image-beat.sh`
#     — k3s の agentd の pool(Deployment agentd-pool)の版を引くかを判じる
#   * Mac 側の agentd の追随 — 同じ問い・別の担い手
#
# ── 何のために読むか
#   image は自分が焼いた doeff の版を label `io.acp.doeff-revision` で名乗る。読み手はその版と
#   「引く先の image が焼いた版」を **この名簿の path に限って** git で比べ、差が在る拍だけ版を引く。
#   指紋や hash をどこにも置かない —— 2 版の差は git の事実ちょうど(第 2 の場所で計算しない)。
#   読み手はこの file を **本線の先端の写しから** 読む(image の中からではない)。image から読むと、
#   この file が着地する前の image を載せた pool が永久に「読めない」で詰む。
#
# ── 読み方
#   1 行 1 つの git の pathspec(repo の根からの相対)。`#` で始まる行と空行は註。
#   読み手は `git diff --quiet <版 a> <版 b> -- <この名簿>` の 3 値(同じ / 違う / 読めない)で判じる。
#   除外は git の除外 pathspec `:(exclude)<綴り>` で書く。⚠ 綴りの末尾は `/**` —— `packages/*/tests/`
#   のように `/` で終える形は **1 file も当たらない**(wildcard が在る pathspec は path 全体への
#   wildmatch で、file の path は `/` で終わらないため。実測 2026-09-19)。
#   ⚠ 読み手へ(穴は 2 つ・向きが逆):
#   1. **語に割れる読み手で読むこと。** 割らない shell(zsh の既定は引用しない変数を語に割らない)で
#      `git diff --quiet <a> <b> -- $paths` と渡すと、git は名簿の全行を **1 個の pathspec** として受け、
#      どの版の対でも「差なし」を返す。症状は**常に same** = 古い agentd が黙って永久に残る側。
#      実弾 2026-09-19: 手で測り直した人が zsh で踏み(効く path を触った着地に「差なし」)、bash で
#      撃ち直して初めて differs になった。sh / bash なら素の `$paths`(引用しない)、Python 等なら
#      list で渡す(shell を通さない)。
#   2. **file 名への展開は止めること**(sh なら `set -f`)—— `*` を含む行が作業 dir 次第で別の語へ
#      化け、除外が黙って効かなくなる。
#   ⇒ sh の読み手は「割る・展開しない」の両方が要る(`set -f` + 引用しない変数)。
#
# ── 変える時
#   ここを 1 行変えれば読み手は全部そろって変わる(読み手の側に写しを置かない)。
#   広げすぎると agentd が要らない入れ替えで落ち、狭めすぎると古い agentd が黙って残る。
#
# ── 初期値の根拠(実測 2026-09-19・9/12 以降の本線 149 commit が触れた file の内訳)
#   packages 765 / docs 259 / .agents 21 / .sisyphus 12 / contracts.lock 11 / tests 7 /
#   .gitignore 6 / .semgrep.yaml 4 / scripts 2 / README.md 2 / doeff 1 / その他 1 ずつ。
#   docs・notes・specs だけの着地を落とし、venv の中身(uv の workspace の member・根の package・
#   依存の集合・interpreter の版)を捉える。tests / scripts / .agents は image には入るが
#   agentd の実行には載らないので外す。
#
# ── 除外の根拠(2026-09-19 の訂正・pr-review なし)
#   上の内訳の「tests 7」は **根の `tests/`**(225 file)の数で、**package の下の tests**
#   (`packages/*/tests/**` = 341 file)はそれとは別の、より大きい塊。`packages/` の 1 行は後者を
#   丸ごと拾うので、「tests は外す」と宣言しながら値は外せていなかった(気づいた人 = ACP 側の読み手の
#   検分)。実測: 9/12 以降の本線 151 commit のうち **7 本が package の下の tests だけ**の着地で、
#   そのたびに pool が要らない入れ替えで転がっていた(排水の上限は手番の上限 = 4 時間)。
#   同じ理由で package の下の docs / examples / README.md も落とす(根のそれらと同じく実行に載らない・
#   この 4 つを足して減るのは 151 中もう 1 本)。狭めすぎの側は反例で見ている ——
#   除外の外が 1 つでも動けば引く(ACP の検 T34 (o))。

packages/
doeff/
pyproject.toml
uv.lock
.python-version
:(exclude)packages/*/tests/**
:(exclude)packages/*/docs/**
:(exclude)packages/*/examples/**
:(exclude)packages/*/README.md
