name: Build doeff-indexer

on:
  workflow_call:
    inputs:
      publish:
        description: "Publish to PyPI"
        required: false
        default: false
        type: boolean
  push:
    tags:
      - 'doeff-indexer-v*'
  workflow_dispatch:
    inputs:
      publish:
        description: 'Publish to PyPI'
        required: false
        default: false
        type: boolean

permissions:
  contents: read

jobs:
  build-linux:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        target: [x86_64, aarch64]
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.target }}
          args: --release --out dist
          working-directory: packages/doeff-indexer
          manylinux: auto
          before-script-linux: |
            # Build the native binary first (without Python feature)
            # For cross-compilation, binary goes to target/<arch>/release/
            if [ "${{ matrix.target }}" = "aarch64" ]; then
              rustup target add aarch64-unknown-linux-gnu
              cargo build --release --no-default-features --target aarch64-unknown-linux-gnu
              mkdir -p python/doeff_indexer/bin
              cp target/aarch64-unknown-linux-gnu/release/doeff-indexer python/doeff_indexer/bin/
            else
              cargo build --release --no-default-features
              mkdir -p python/doeff_indexer/bin
              cp target/release/doeff-indexer python/doeff_indexer/bin/
            fi

      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: doeff-indexer-wheels-linux-${{ matrix.target }}
          path: packages/doeff-indexer/dist/*.whl

  build-linux-freethreaded:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        target: [x86_64, aarch64]
    steps:
      - uses: actions/checkout@v4
      - name: Build free-threaded wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.target }}
          args: --release --out dist -i python3.13t python3.14t
          working-directory: packages/doeff-indexer
          manylinux: auto
          before-script-linux: |
            if [ "${{ matrix.target }}" = "aarch64" ]; then
              rustup target add aarch64-unknown-linux-gnu
              cargo build --release --no-default-features --target aarch64-unknown-linux-gnu
              mkdir -p python/doeff_indexer/bin
              cp target/aarch64-unknown-linux-gnu/release/doeff-indexer python/doeff_indexer/bin/
            else
              cargo build --release --no-default-features
              mkdir -p python/doeff_indexer/bin
              cp target/release/doeff-indexer python/doeff_indexer/bin/
            fi
      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: doeff-indexer-wheels-linux-freethreaded-${{ matrix.target }}
          path: packages/doeff-indexer/dist/*.whl

  build-macos:
    runs-on: macos-latest
    strategy:
      matrix:
        target: [x86_64, aarch64]
    steps:
      - uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}-apple-darwin

      - name: Build native binary
        working-directory: packages/doeff-indexer
        run: |
          cargo build --release --no-default-features --target ${{ matrix.target }}-apple-darwin
          mkdir -p python/doeff_indexer/bin
          cp target/${{ matrix.target }}-apple-darwin/release/doeff-indexer python/doeff_indexer/bin/

      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.target }}
          args: --release --out dist
          working-directory: packages/doeff-indexer

      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: doeff-indexer-wheels-macos-${{ matrix.target }}
          path: packages/doeff-indexer/dist/*.whl

  build-macos-freethreaded:
    runs-on: macos-latest
    strategy:
      matrix:
        target: [x86_64, aarch64]
    steps:
      - uses: actions/checkout@v4
      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}-apple-darwin
      - name: Setup free-threaded Python
        uses: actions/setup-python@v5
        with:
          python-version: |
            3.13t
            3.14t
      - name: Build native binary
        working-directory: packages/doeff-indexer
        run: |
          cargo build --release --no-default-features --target ${{ matrix.target }}-apple-darwin
          mkdir -p python/doeff_indexer/bin
          cp target/${{ matrix.target }}-apple-darwin/release/doeff-indexer python/doeff_indexer/bin/
      - name: Build free-threaded wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.target }}
          args: --release --out dist --find-interpreter
          working-directory: packages/doeff-indexer
      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: doeff-indexer-wheels-macos-freethreaded-${{ matrix.target }}
          path: packages/doeff-indexer/dist/*.whl

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Build native binary
        working-directory: packages/doeff-indexer
        shell: bash
        run: |
          cargo build --release --no-default-features
          mkdir -p python/doeff_indexer/bin
          cp target/release/doeff-indexer.exe python/doeff_indexer/bin/

      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          args: --release --out dist
          working-directory: packages/doeff-indexer

      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: doeff-indexer-wheels-windows
          path: packages/doeff-indexer/dist/*.whl

  build-windows-freethreaded:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup free-threaded Python
        uses: actions/setup-python@v5
        with:
          python-version: |
            3.13t
            3.14t
      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
      - name: Build native binary
        working-directory: packages/doeff-indexer
        shell: bash
        run: |
          cargo build --release --no-default-features
          mkdir -p python/doeff_indexer/bin
          cp target/release/doeff-indexer.exe python/doeff_indexer/bin/
      - name: Build free-threaded wheels
        uses: PyO3/maturin-action@v1
        with:
          args: --release --out dist --find-interpreter
          working-directory: packages/doeff-indexer
      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: doeff-indexer-wheels-windows-freethreaded
          path: packages/doeff-indexer/dist/*.whl

  # Build source distribution
  sdist:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build sdist
        uses: PyO3/maturin-action@v1
        with:
          command: sdist
          args: --out dist
          working-directory: packages/doeff-indexer

      - name: Upload sdist
        uses: actions/upload-artifact@v4
        with:
          name: doeff-indexer-sdist
          path: packages/doeff-indexer/dist/*.tar.gz

  publish:
    name: Publish to PyPI
    needs: [build-linux, build-linux-freethreaded, build-macos, build-macos-freethreaded, build-windows, build-windows-freethreaded, sdist]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/doeff-indexer-v') || (github.event_name == 'workflow_dispatch' && inputs.publish) || (github.event_name == 'workflow_call' && inputs.publish)
    steps:
      - name: Download doeff-indexer artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: doeff-indexer-*
          path: dist
          merge-multiple: true

      - name: List artifacts
        run: ls -la dist/

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist/
          skip_existing: true
          password: ${{ secrets.PYPI_API_TOKEN }}
