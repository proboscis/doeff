添付図の局所修正。上下段の結果枠へ向かう矢印は削除して、各結果枠の見出しをそれぞれ「greet_asyncの最終結果」「greetの最終結果」にする。下のhanakoハンドラ内の「ユーザーから名前を取得する」は誤りなので「固定の名前『花子』を返す」へ変更。上段のコード全行に短いコメントを追加し、必要ならコード枠を大きくする。
async def greet_async():  # 挨拶を作る
    name = await read_name_async()  # 名前を受け取る
    return f"こんにちは、{name}さん"  # 挨拶を返す
async def read_name_async():  # 名前の供給元
    return "花子"  # 固定の名前を返す
下段のコードと他の矢印・説明・配色・完全に不透明な白背景は保持。
