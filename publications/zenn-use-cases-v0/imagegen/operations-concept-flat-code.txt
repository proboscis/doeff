Use case: infographic-diagram. 日本語技術記事の完成ダイアグラム。添付は表現トーンの参考のみ。全面が不透明な白、ネイビーの日本語、ティールの明瞭な矢印、薄い青/ミント/紫の平面的な角丸枠。写実・3D・金属・ガラス・立体的装置・台座・装飾的なイラストを禁止。横長3:2。図中に下記の短いコードを大きく正確な等幅文字で配置し、対応する操作や流れと矢印で結ぶ。大きいタイトル、余白、最大6ノード程度。日本語の説明を短く、コードは改変しない。原稿からの抜粋なので小さく「コードは抜粋。完全な例は本文へ」。以下の意味を守り、勝手な挙動や架空のAPIを追加しない。
コードは提供されたもののみ。コード各行の日本語コメントも省略せず表示。機械的な循環矢印や図にないAPIを追加しない。
見出し:操作の依頼を、3つのハンドラへ渡す
コード:
p_combined = mock_handlers(runtime=combined_git)(propose_and_notify(work_dir))  # Gitを解釈する
p_combined = notifications_only(combined_notify)(p_combined)  # 通知を解釈する
p_combined = in_memory_handlers(seed_data={"notice-signing-key": b"demo-key"})(p_combined)  # 鍵を解決する
図:白い不透明背景、平面の青・ミント・紫の角丸枠、紺文字、青緑の矢印。写真・3D禁止。上に3行のコードを等幅で正確に配置する。下は左の「同じProgram」から3種類の依頼が、それぞれ独立の矢印で右の担当ハンドラへ進む対応図。「Gitの依頼 → mock_handlers → PRHandle」「通知の依頼 → notifications_only → 通知の記録」「GetSecret → in_memory_handlers → bytesの鍵」。3種類の結果が同時に全ハンドラから返るような合流は描かない。通知ハンドラの下に短い注記「通知以外はPass(effect, k)」。現行版のコードで使う順序・役割を示すので、任意の順序で積めるとは書かない。メモリ上の動作とコードの各行を対応させる。
説明:Git・秘密情報・通知の依頼を分け、テストではそれぞれメモリのハンドラで解釈します。
