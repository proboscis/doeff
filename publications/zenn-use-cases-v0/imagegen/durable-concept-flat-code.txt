Use case: infographic-diagram. 日本語技術記事の完成ダイアグラム。添付は表現トーンの参考のみ。全面が不透明な白、ネイビーの日本語、ティールの明瞭な矢印、薄い青/ミント/紫の平面的な角丸枠。写実・3D・金属・ガラス・立体的装置・台座・装飾的なイラストを禁止。横長3:2。図中に下記の短いコードを大きく正確な等幅文字で配置し、対応する操作や流れと矢印で結ぶ。大きいタイトル、余白、最大6ノード程度。日本語の説明を短く、コードは改変しない。原稿からの抜粋なので小さく「コードは抜粋。完全な例は本文へ」。以下の意味を守り、勝手な挙動や架空のAPIを追加しない。
コードは提供されたもののみ。コード各行の日本語コメントも省略せず表示。機械的な循環矢印や図にないAPIを追加しない。
見出し:完了した計算の結果を、次の実行へ渡す
コード:
@cache()  # 同じ本文と版の保存結果を使う
@do  # 解析をProgramにする
def parse_document(text: str, parser_version: str):  # 本文と版で識別
図:白背景・平面的な角丸枠・紺文字・青緑の矢印。上に3行のデコレータと関数ヘッダの抜粋コードを大きく掲載。「関数本体は本文へ」と注記。下に「finish()を先頭から実行」→「parse_documentのMemo依頼」→ひし形「保存結果はある？」。あるなら「保存した解析3行を取得」、ないなら「解析する→完了結果をSQLiteへ保存」。どちらからも「見出し抽出へ進む」に合流する。保存先は平面のSQLiteラベル枠。生きたPythonスタックや途中の命令を保存する図は描かない。キャッシュの識別は「関数名＋本文＋処理の版」。3Dや写真・立体物禁止。
説明:`finish()`を先頭から実行し直しても、保存済みの解析結果を受け取って見出し抽出へ進めます。保存するのは完了した結果です。
矢印は意味が確かなものだけ。値の受け渡し方向は厳密に守る。
