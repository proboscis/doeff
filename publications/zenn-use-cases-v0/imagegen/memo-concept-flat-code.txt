Use case: infographic-diagram. 日本語技術記事の完成ダイアグラム。添付は表現トーンの参考のみ。全面が不透明な白、ネイビーの日本語、ティールの明瞭な矢印、薄い青/ミント/紫の平面的な角丸枠。写実・3D・金属・ガラス・立体的装置・台座・装飾的なイラストを禁止。横長3:2。図中に下記の短いコードを大きく正確な等幅文字で配置し、対応する操作や流れと矢印で結ぶ。大きいタイトル、余白、最大6ノード程度。日本語の説明を短く、コードは改変しない。原稿からの抜粋なので小さく「コードは抜粋。完全な例は本文へ」。以下の意味を守り、勝手な挙動や架空のAPIを追加しない。
コードは提供されたもののみ。コード各行の日本語コメントも省略せず表示。機械的な循環矢印や図にないAPIを追加しない。
見出し:保存先を重ね、同じMemo操作を渡す
コード:
program = memo_handler(l1, name="L1")(read_answer())  # 本体に最も近い層
program = memo_handler(l2, name="L2")(program)  # L1の次の問い合わせ先
program = memo_handler(l3, name="L3")(program)  # 最後の問い合わせ先
図:白背景の平面的な概念図。左に3行のコードをそのまま示す。右は外側のL3の枠がL2の枠を含み、L2の枠がL1の枠を含み、L1がread_answer()の小枠を含むという3層の入れ子を示す。問い合わせの順序は、図の下の独立した横一列『処理 → L1 → L2 → L3』で明示し、矢印に『見つからなければ外へ』と付ける。タイトルの直下に『処理はMemoGet、保存先はハンドラが決める』。補足は『この例の3層はすべてメモリ。保存先は交換できる』。3層の同時問い合わせや並列処理を描かない。3D・写実・装置禁止。コード行と対応する層を小さな番号1・2・3または同色で対応させる。
説明:処理に近いL1からL2、L3へ問い合わせます。見つかった値は、戻る途中で手前の空の層へ保存されます。
矢印は意味が確かなものだけ。値の受け渡し方向は厳密に守る。
