Use case: precise-object-edit. 添付画像は編集対象。この記事に使用する完成画像の局所的修正。
3つのモジュール上の「チャット」「構造化データ」「ベクトル」というラベルを、左から「OpenAI」「Gemini」「OpenRouter」へ置き換える。アイコンはそれぞれ中立的な接続モジュールのアイコンへ変更。依頼から選択可能なプロバイダへつながる図であり、機能ごとの専用装置の図ではない。中央の依頼と見出し、暗い背景、全体の配置は維持する。
出力は透明背景ではない。アルファ抜きや背景除去を行わず、画像全体を完全に不透明にする。日本語を正確に保つ。新しい説明文や装飾を加えない。
