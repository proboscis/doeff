Use case: infographic-diagram. 日本語技術記事の完成ダイアグラム。添付は表現トーンの参考のみ。全面が不透明な白、ネイビーの日本語、ティールの明瞭な矢印、薄い青/ミント/紫の平面的な角丸枠。写実・3D・金属・ガラス・立体的装置・台座・装飾的なイラストを禁止。横長3:2。図中に下記の短いコードを大きく正確な等幅文字で配置し、対応する操作や流れと矢印で結ぶ。大きいタイトル、余白、最大6ノード程度。日本語の説明を短く、コードは改変しない。原稿からの抜粋なので小さく「コードは抜粋。完全な例は本文へ」。以下の意味を守り、勝手な挙動や架空のAPIを追加しない。
コードは提供されたもののみ。コード各行の日本語コメントも省略せず表示。機械的な循環矢印や図にないAPIを追加しない。
見出し:10秒で再開し、次に20秒で再開する
コード:
slow = yield Spawn(worker(20))  # 20秒の待機を開始
fast = yield Spawn(worker(10))  # 10秒の待機も開始
finished = yield Gather(slow, fast)  # slow、fastの順で日時を受け取る
図:白背景の平面的なタイムライン。上部に3行のコード。図の横軸は「仮想時刻 0秒・10秒・20秒」。2本の行を「slow：20秒待つ」「fast：10秒待つ」とラベル。両方の開始点は0秒、fastは10秒で再開、slowは20秒で再開。10秒のfast完了から20秒のslow完了へ1本の時刻進行矢印で順序を明示し、fastが20秒まで待つ図にはしない。右端に「Gatherの結果は指定順」の枠と「開始からの秒数：[20.0, 10.0]」。Gatherが直接秒数を返す誤解を防ぐため「日時の差を秒へ変換」と短く示す。下部に注記「通常タスクが待機したら、次の予定へ時計を進める」。コードと該当する行を色で対応。実時間20秒待つ図ではなく、仮想時刻の推移を示す。写真・3D・立体物禁止。
説明:20秒のタスクと10秒のタスクが同じ仮想時刻に待機を始めます。10秒のタスクが先に再開し、最後にGatherが指定順で日時を返します。開始からの秒数へ変換すると`[20.0, 10.0]`です。
