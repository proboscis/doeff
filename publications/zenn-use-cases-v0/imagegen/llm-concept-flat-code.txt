Use case: infographic-diagram. 日本語技術記事の完成ダイアグラム。添付は表現トーンの参考のみ。全面が不透明な白、ネイビーの日本語、ティールの明瞭な矢印、薄い青/ミント/紫の平面的な角丸枠。写実・3D・金属・ガラス・立体的装置・台座・装飾的なイラストを禁止。横長3:2。図中に下記の短いコードを大きく正確な等幅文字で配置し、対応する操作や流れと矢印で結ぶ。大きいタイトル、余白、最大6ノード程度。日本語の説明を短く、コードは改変しない。原稿からの抜粋なので小さく「コードは抜粋。完全な例は本文へ」。以下の意味を守り、勝手な挙動や架空のAPIを追加しない。
コードは提供されたもののみ。コード各行の日本語コメントも省略せず表示。機械的な循環矢印や図にないAPIを追加しない。
見出し: LLMへの依頼を、ハンドラで解釈する
コード:
@do  # 合成できる処理を作る
def summarize(text, model):  # 文章とモデルを受け取る
    return (yield LLMChat(  # 応答を依頼して返す
        messages=[{"role": "user", "content": text}], model=model,  # 入力とモデル
    ))  # 指定した依頼を閉じる
図の意味と流れ: 左から 処理本体 → yield LLMChat → 取り付けたハンドラ → 応答を返して続きを再開。ハンドラの選択肢として「OpenAI」「Gemini」「OpenRouter」「テスト用」を横方向の分岐で示す。どれかを選ぶ図であり、同時実行ではない。下に「対応機能・返り値はハンドラごとに確認」と明記。4種のLLM機能を全プロバイダが実装済みと見せない。コードは通常チャットの依頼だけを示す。
説明: 共通のLLMChatを、取り付けたハンドラが解釈します。対応機能と返り値はハンドラごとに確認します。
