Use case: infographic-diagram. 日本語技術記事の完成ダイアグラム。添付は表現トーンの参考のみ。全面が不透明な白、ネイビーの日本語、ティールの明瞭な矢印、薄い青/ミント/紫の平面的な角丸枠。写実・3D・金属・ガラス・立体的装置・台座・装飾的なイラストを禁止。横長3:2。図中に下記の短いコードを大きく正確な等幅文字で配置し、対応する操作や流れと矢印で結ぶ。大きいタイトル、余白、最大6ノード程度。日本語の説明を短く、コードは改変しない。原稿からの抜粋なので小さく「コードは抜粋。完全な例は本文へ」。以下の意味を守り、勝手な挙動や架空のAPIを追加しない。
コードは提供されたもののみ。コード各行の日本語コメントも省略せず表示。機械的な循環矢印や図にないAPIを追加しない。
見出し:待ち受けを登録してから、イベントを発行する
コード:
event = yield WaitForEvent(PageReady, PagesFinished)  # 次の入力か終了を待つ
if isinstance(event, PagesFinished):  # 終了通知なら収集を終える
    return titles  # 受け取ったタイトル列を返す
titles = (*titles, event.title)  # タイトルを加えて次の待機へ
図:白背景の平面ダイアグラム。上にread_until_endのループ本体からの4行のコードを、コメント込みで等幅表示する。供給側は仮想時刻1秒PageReady("はじめに")、2秒PageReady("遊び方")、3秒PagesFinished()をこの順にPublish。中央受信側は「WaitForEventで購読を登録」→「型で分岐」。PageReadyなら「titlesへ追加」→登録へ戻る矢印。PagesFinishedなら「タイトル2件を返す」で終了。右にRaceの2つのケースを混同せず小さく示す: 期限5秒→3秒でタイトル2件、期限0.5秒→入力より先に「期限切れ」。いずれも所有側finallyのCancelで不要なTaskを止める。底部に「購読前のイベントを保存する保証はない」。写真・3D・ストックイラストや装飾的機械は使わない。
説明:組み込みハンドラは永続キューではありません。購読前のイベントを後から受け取る保証はありません。
