Use case: infographic-diagram. 日本語技術記事の完成ダイアグラム。添付は表現トーンの参考のみ。全面が不透明な白、ネイビーの日本語、ティールの明瞭な矢印、薄い青/ミント/紫の平面的な角丸枠。写実・3D・金属・ガラス・立体的装置・台座・装飾的なイラストを禁止。横長3:2。図中に下記の短いコードを大きく正確な等幅文字で配置し、対応する操作や流れと矢印で結ぶ。大きいタイトル、余白、最大6ノード程度。日本語の説明を短く、コードは改変しない。原稿からの抜粋なので小さく「コードは抜粋。完全な例は本文へ」。以下の意味を守り、勝手な挙動や架空のAPIを追加しない。
コードは提供されたもののみ。コード各行の日本語コメントも省略せず表示。機械的な循環矢印や図にないAPIを追加しない。
見出し: 生成と編集を、ひとつの制作処理へ
コード:
@do  # 制作の順序をProgramにする
def create_cover(model):  # 使うモデル名を受け取る
    generated = yield generate_cover(model)  # 生成結果を受け取る
    edited = yield brighten_cover(generated, model)  # 画像を編集へ渡す
    return edited  # 編集後のImageResultを返す
図の意味と流れ: 白背景・平面・紺文字・青緑の矢印。左にコメント付きコード、右に縦方向の処理図。generate_cover → ImageGenerate → 選択した画像ハンドラ → ImageResult。そこからimagesという名前の矢印でbrighten_coverのImageEditへ渡し、編集後のImageResultへ進む。ハンドラの選択肢は「Gemini」「Seedream」「テスト用の固定応答」の3つ、どれかを選ぶことを明記。3D・写真・立体的な装置・雰囲気だけの絵は禁止。API境界とデータの受け渡しをコード行と対応させる。
説明: 生成と編集を別の依頼として表し、生成結果の画像を次の編集へ渡します。通信方法は取り付けるハンドラが担当します。
