Use case: infographic-diagram. 日本語技術記事の完成ダイアグラム。添付は表現トーンの参考のみ。全面が不透明な白、ネイビーの日本語、ティールの明瞭な矢印、薄い青/ミント/紫の平面的な角丸枠。写実・3D・金属・ガラス・立体的装置・台座・装飾的なイラストを禁止。横長3:2。図中に下記の短いコードを大きく正確な等幅文字で配置し、対応する操作や流れと矢印で結ぶ。大きいタイトル、余白、最大6ノード程度。日本語の説明を短く、コードは改変しない。原稿からの抜粋なので小さく「コードは抜粋。完全な例は本文へ」。以下の意味を守り、勝手な挙動や架空のAPIを追加しない。
コードは提供されたもののみ。コード各行の日本語コメントも省略せず表示。機械的な循環矢印や図にないAPIを追加しない。
見出し: 待機する対象と、解釈する依頼を分ける
コード:
@do  # 挨拶のProgramを作る
def greet():  # 名前を取得して挨拶する
    name = yield ReadName()  # ハンドラから名前を受け取る
    return f"こんにちは、{name}さん"  # 挨拶を返す
図の意味と流れ: 白背景、平面的な2段の比較図。写真、3D、立体装置を使わない。上段は『coroutineの実行』。greet_async内の『await read_name_async()』から『read_name_async』へ呼び出す矢印、そこから『花子』を返す矢印でgreet_asyncへ戻る。外側の薄い枠を『この例ではasyncioのTaskが実行を管理』とする。下段は『依頼の解釈』。greet内の『yield ReadName()』から『ReadNameという依頼』、それから『hanakoハンドラ』へ依頼の矢印。ハンドラから『Resume(k, 花子)』で元のgreetへ戻る別の矢印を引く。下段外枠に『Rust VMがハンドラ境界と継続を扱う』。上下とも最終結果を『こんにちは、花子さん』とする。コードパネルは下段のgreetの正確な4行。asyncioには非同期以外の処理が書けない、awaitで必ずTask切り替えが起こる、と読めるラベルを入れない。
説明: awaitはawaitableを進める構文です。doeffでは依頼のデータと継続を、実行時に取り付けたハンドラへ渡します。
図内にResumeのコードを書く場合は正確に Resume(k, "花子") と引用符を付ける。
