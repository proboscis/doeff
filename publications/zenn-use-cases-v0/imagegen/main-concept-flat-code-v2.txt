Use case: infographic-diagram. 日本語技術記事の完成ダイアグラム。添付は表現トーンの参考のみ。全面が不透明な白、ネイビーの日本語、ティールの明瞭な矢印、薄い青/ミント/紫の平面的な角丸枠。写実・3D・金属・ガラス・立体的装置・台座・装飾的なイラストを禁止。横長3:2。図中に下記の短いコードを大きく正確な等幅文字で配置し、対応する操作や流れと矢印で結ぶ。大きいタイトル、余白、最大6ノード程度。日本語の説明を短く、コードは改変しない。原稿からの抜粋なので小さく「コードは抜粋。完全な例は本文へ」。以下の意味を守り、勝手な挙動や架空のAPIを追加しない。
コードは提供されたもののみ。コード各行の日本語コメントも省略せず表示。機械的な循環矢印や図にないAPIを追加しない。
見出し: 同じAskに、ハンドラが違う名前を返す
コード:
@do  # 挨拶の手順を組み立てる
def greet():  # 名前の供給は外側へ委ねる
    name = yield Ask("name")  # 名前を受け取る
    return f"こんにちは、{name}さん"  # 挨拶を返す
図の意味と流れ: 白背景、濃いネイビーの日本語、ティールの矢印、淡い色の平面の角丸ボックス。画像上部に見出し、左上にPythonコードパネル、右または下に同じgreet()から作った2つの計算を上下2段で比較する図。上段はgreetのAsk("name")→lazy_askのenv={"name": "花子"}→値「花子」がgreetへ戻る→結果「こんにちは、花子さん」。下段は同じgreetのAsk("name")→lazy_askのenv={"name": "太郎"}→値「太郎」がgreetへ戻る→結果「こんにちは、太郎さん」。両段のgreetコードは共通で、別々の実行であることを「同じ手順・別の実行」と添える。一つの依頼を同時に両ハンドラへ送る分岐図にしない。3D、写実的な装置、質感、立体台座は使わない。
説明: 同じ`greet`から作った2つの計算へ、それぞれ名前を供給します。`Ask`から文字列を受け取る契約を保ち、本体を変えずに結果を変えています。
