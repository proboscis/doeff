Use case: infographic-diagram. 日本語技術記事の完成ダイアグラム。添付は表現トーンの参考のみ。全面が不透明な白、ネイビーの日本語、ティールの明瞭な矢印、薄い青/ミント/紫の平面的な角丸枠。写実・3D・金属・ガラス・立体的装置・台座・装飾的なイラストを禁止。横長3:2。図中に下記の短いコードを大きく正確な等幅文字で配置し、対応する操作や流れと矢印で結ぶ。大きいタイトル、余白、最大6ノード程度。日本語の説明を短く、コードは改変しない。原稿からの抜粋なので小さく「コードは抜粋。完全な例は本文へ」。以下の意味を守り、勝手な挙動や架空のAPIを追加しない。
コードは提供されたもののみ。コード各行の日本語コメントも省略せず表示。機械的な循環矢印や図にないAPIを追加しない。
見出し:処理は同じ、実行戦略を差し替える
コード:
@do  # 集合の処理もProgramにする
def pipeline():  # 実行順は外で決める
    collection = yield Traverse(process, [1, 2, 3])  # 3件を依頼
    items = yield Inspect(collection)  # 入力順に結果を得る
    return [item.value for item in items]  # [10, 20, 30]
図:白背景の平面的な説明図。3D・実写・立体台座は禁止。上部にコード枠を置く。processは「入力値と同じ秒数のDelayの後、10倍を返す」と図中に明記。中央のTraverse依頼から、実行時にどちらか一方だけを選ぶ分岐へ矢印を引く。上段sequential()は1秒→2秒→3秒の横一列、仮想時間合計6秒。下段parallel(concurrency=3)は同時に始まる1秒・2秒・3秒の3レーン、仮想時間合計3秒。両方の終点は同じ入力順の[10, 20, 30]。分岐を同時実行や後処理の段階として描かない。実時間の速度測定という文言を入れない。
説明:`Traverse(process, [1, 2, 3])`という同じ依頼を、逐次か最大3並行かのどちらかで解釈します。どちらも結果は入力順の10・20・30です。
