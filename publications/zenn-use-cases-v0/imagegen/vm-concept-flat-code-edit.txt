添付画像を局所修正。白背景と平面ダイアグラムを維持、背景完全不透明。右側「ハンドラ（Rust側）」を「ハンドラ（Python）」へ変更。"Alice"のコードを `Resume(k, "花子")` へ変更。その下のラベル「ハンドラが返す結果」を「VMへ再開を依頼」へ変更。下の戻り矢印の先頭を右ハンドラから中央VMへ戻る矢印へ変更し、その次に中央VMから左のPython本体へ矢印を描く。VMを通じてPython generatorへ値を送り再開する。その他は維持。
