# 執筆完了の照合記録

2026-09-16、記事別エージェントの修正を統合し、最終原稿・実行例・画像を確認した。
対象はdoeffリポジトリ所有の日本語Zenn記事シリーズ。投稿・公開や製品ランタイムの変更は含めない。
参照実装は開発checkout `d4705914e39740aee98a9f57a4535c463d9479cc`。

## 要求と成果物

| 要求 | 成果物・確認方法 | 結果 |
| --- | --- | --- |
| 「doeffとは？」を入口にし、短いコードを早く見せる | [メイン](doeff-main.md)の定義直後にAsk・最小ハンドラ・設置・実行結果を示す例。悩みと解法は表に統一。22個別記事への導線、Agentsを先頭にした用途紹介、全記事の戻り先 | 対応済み |
| 実行方法をハンドラで交換し、テスト・別基盤へ接続する価値 | HTTP・Memo・時間・ドメイン操作を別のハンドラとして合成。本文のProgramを維持したまま取得・保存・待機を交換する例 | 対応済み |
| 全機能にコードを付け、実用的な内容にする | [43機能の対応表](feature-examples.md)、[31パッケージの棚卸し](package-coverage.md)。各機能に本文のコードと検証範囲を対応付けた | 対応済み |
| 記事ごとの独立した精査 | 全23記事へ担当エージェントを割り当て、実装・呼び出し・戻り値・制約を照合。[担当表と個別記録](reviews/README.md) | 対応済み |
| 合成処理を@doで書く | collect_openai_streamを含む補助処理を@doとyieldで合成。SDKのanextとの接点をAwaitにし、チャンク・終了・エラーを確認 | 対応済み |
| HTTPとMemoのハンドラを組み合わせる | [記録・再利用](doeff-replay.md)ではclient_factory注入を除去。固定HTTPで初回取得、SQLiteで保存、拒否HTTPへ交換して再利用、保存ミスでLookupError | 対応済み |
| 各行へ目的と期待結果のコメント | 本文と完全なPython例の実質4,101行に日本語コメント。機械検査はコメントの存在、各担当は意味を確認。Hy・CLIも担当が照合 | 対応済み |
| coroutine・ハンドラ合成・Rust VMの追加ページ | [coroutine](doeff-coroutines.md)、[ハンドラ合成](doeff-handlers.md)、[Rust VM](doeff-vm.md)を独立記事として追加し、専用例を実行 | 対応済み |
| 深い設計・Hyまで説明する | [境界設計](doeff-boundaries.md)で副作用とドメインAPIの両方を扱い、[Hy](doeff-hy.md)でマクロ・契約・条件分岐・収集・テストを実行 | 対応済み |
| 非公開repoは概念のみ利用する | 架空のカードゲーム・文書処理として独立した例を作成。公開原稿・例・採用プロンプト・図の仕様に元repo・会社・運用環境の識別子がないことを検索確認 | 対応済み |
| 各記事の概念図と処理図をimagegenで制作 | 概念・処理図46枚と冒頭キービジュアル1枚を組み込みimagegenで生成・編集。白い不透明背景・平面図・紺文字・矢印・短いコードとコメント。[制作記録](imagegen/README.md) | 対応済み |
| 画像の正確さ・保存・リンク | 全47枚を個別に目視。概念・処理図は1536×1024、冒頭画像は横長。全画像が各3MB未満、不透明。採用ハッシュ・プロンプト・本文caption・画像台帳が一致 | 対応済み |
| repoを正本としてTODO・レビュー・プレビューを残す | 原稿・例・図・記録をpublicationsとimagesへ保存。[TODO](TODO.md)と本記録を最終結果に更新。画像・コードを埋め込んだHTMLを生成して表示確認 | 対応済み |

## 最終一括検査

以下は、記事別の修正と各行コメントの補足後に実行した。

```bash
uv run --no-sync python publications/zenn-use-cases-v0/verify_examples.py  # 21本の例と、別プロセス・原稿・図・コメントを一括検査する。
uv run --no-sync ruff check publications/zenn-use-cases-v0/*.py publications/zenn-use-cases-v0/examples/*.py  # 原稿用Pythonに静的検査の違反がないことを確認する。
git diff --check  # 追跡済みファイルの差分に空白エラーがないことを確認する。
```

すべて成功。一括検査の内訳は次のとおり。

- 21本のオフライン実行例。
- SQLiteを実3プロセスで開き、既存結果を再利用する検査。
- Hy・ADR・Semgrepの例。
- 実エージェントを起動しないConductor履歴再利用。
- 23記事・31パッケージ・43機能・47画像の対応、リンク、画像ハッシュ、caption。
- メイン本文2例の順次実行、最小ハンドラ、別キー・別型の依頼の委譲、22記事すべての表リンク。
- 本文Python138ブロックの構文と、本文・完全な例の実質4,101行の日本語コメント。

記事別の追加テストと実行結果は[各レビュー](reviews/README.md)を参照。コメントの意味や説明の正しさを、構文検査だけで証明したとは扱わない。

最終原稿から`/tmp/doeff-zenn-preview.html`を再生成し、ブラウザで開いた。描画した画面で、メイン冒頭の短いコードと記事選択の表示を確認した。原稿と画像の正本はリポジトリ内に保持する。

## 既存実装の不整合と検証範囲

原稿の検査が通ったことと、全パッケージがそのまま本番動作することは区別する。

- doeff-notifyのtesting委譲に旧Pass()が残る。記事では対象を選別し、現行のPass(effect, k)で外側へ渡す。
- doeff-secretの未対応分岐に削除済みDelegate()が残る。環境変数からクラウドへの自動fallbackは動作確認済みとしない。
- doeff-flowはRunResultのimportに失敗する。対応する例の実行不可を明記した。
- doeff-ml-nexusの上位ファクトリはHyの契約不足でimportに失敗する。記事別に実行した既存Docker/MLテストは11成功・7失敗。7件はwriter外側のstate不足であり、全テスト成功とは報告しない。記事の正しい構成・固定ハンドラ・Programのシリアライズ往復は成功した。

この作業では製品ランタイムの修正をしていない。実エージェント、LLM・画像の有料API、実Git操作・通知送信、実Docker・SSH・GPU、クリーンインストール、外部操作の強制終了からの復旧は未実行。ScenarioAgentHandlerの固定結果を、本番認証やスキーマ再試行の実証として扱わない。履歴・完了結果の再利用と、生きた継続の保存も区別した。

## 従前の完了判定の訂正

旧記録の20記事・40画像は今回の成果物を表さない。Graphvizだけでimagegen要求を満たした判定、写実的な旧画像、合成処理をasync helperへ閉じ込めた例、HTTPクライアントのfactory注入をハンドラ交換の主例とした構成を訂正した。

現在の成果物は23記事・47枚のimagegen画像と、実装照合済みのコード例である。旧画像は保存し、採用画像は[台帳](imagegen/manifest.json)で一意に示す。判断の理由と戻し方は[編集判断の記録](../../docs/design/decisions/2026-09-15-zenn-use-cases.md)に残した。

## 最小ハンドラと悩みの表への修正（2026-09-16追記）

冒頭に@handler・@do付きのhandle_ask_effect(effect, k)を明示し、AskとResumeの対応、担当外のPass、with_handlersでの設置、runの結果をその場で示した。悩みと解法を表へ揃え、22個別記事すべてに表内リンクを設けた。メインの図2枚も同じコードへ更新し、組み込みimagegenで生成・編集して目視確認した。最終一括検査と対象PythonのRuffは成功。詳細は[追加レビュー](reviews/main.md)。

## 冒頭のキービジュアル（2026-09-16追記）

ユーザーの配置指示に合わせ、定義の前へmain-hero.pngを追加した。組み込みimagegenで生成した横長の白背景の図で、大きいdoeffの文字、AskとResumeの往復、短いコメント付きコードを示す。既存の最小ハンドラと表は維持。画像より先にコードがあることを求める旧検査は、冒頭画像→定義→最小コード→詳しい概念図の順を確認する検査へ更新した。画像台帳は47枚に更新した。

画像追加後の確認はverify_publication.py、reviews/main-check.py、変更した検証スクリプトのRuff、git diff --check。すべて成功した。1672×941・1,234,645バイト・不透明のPNGを保存し、ブラウザの描画でタイトル直下の配置を確認して、更新したプレビューを開いた。
