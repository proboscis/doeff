[package]
name = "doeff-vm-core"
version = "0.1.0"
edition = "2021"
rust-version = "1.74"
description = "Core VM machinery for doeff"
license = "MIT"
repository = "https://github.com/proboscis/doeff"

[lib]
name = "doeff_vm_core"
crate-type = ["rlib"]

[features]
default = []
python_bridge = ["dep:pyo3"]
vm_debug_logs = []
# Runtime invariant checks after every VM step (debug/reference builds only).
# See docs/crystallization/invariants.md for the conditions.
invariant-checks = []

[dependencies]
pyo3 = { version = "0.28", features = ["py-clone"], optional = true }

[dev-dependencies]
criterion = "0.5"
pyo3 = { version = "0.28", features = ["auto-initialize", "py-clone"] }

[[bench]]
name = "dispatch"
harness = false
required-features = ["python_bridge"]
