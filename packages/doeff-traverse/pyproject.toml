[project]
name = "doeff-traverse"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "doeff",
    "doeff-core-effects",
]

[dependency-groups]
test = [
    "doeff-time",
]

[tool.uv.sources]
doeff = { workspace = true }
doeff-core-effects = { workspace = true }
doeff-time = { workspace = true }
