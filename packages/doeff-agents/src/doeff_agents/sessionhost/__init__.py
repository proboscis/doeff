"""Session-host effect vocabulary + shared policy program (ADR-DOE-AGENTS-004 C1).

The submodules are Hy modules; importing this package registers the Hy import
hook so ``doeff_agents.sessionhost.effects`` / ``.policy`` resolve normally.
"""

import hy  # noqa: F401
