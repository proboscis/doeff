# land-queue 宣言(dotfiles ADR-DOTFILES-017 の機構、repo-local 方策)。
# 敷設 2026-08-09(land-queue-all-repos-rollout 便 — operator 指示「全部の repo で
# 利用したい」+ operator 決裁 sha d54350a8)。mixed repo(proboscis/doeff* は
# company-mixed-repos.txt 登載)— 編集は任意席・著述は個人系。
#
# upstream repo 規約の機械化(発注 (4)): 「検証済みでしか main を進めない」は
# 2026-08-17 の operator 裁定で**着地の門から事後検証へ移った**(下の mode = "focus"
# の頭注)。テストは減らさない — 走る場所が変わる(日次全量検査 + stable 昇格)。
# battery 実測 2026-08-09: uv run pytest -m "not e2e" = 1294 passed / 78 skipped /
# 8 deselected(11m45s)。e2e marker は実 API キー依存につき gate 外(席実測の
# 分担)。make sync 前置は AGENTS.md 明文(uv sync は Rust VM を確実に再 build
# しない — stale binary の phantom failure 防止)。
# 敷設時に main の実赤 1 本(enforcement-ledger semgrep_rules 246≠247 の記帳漏れ)
# を bootstrap 便で是正済み(テスト自身の処方 = 増加は台帳を上げる)。

[land-queue]
enabled = true
base = "main"
# 区画の名前(着地の台帳の repo 鍵・pod の窓が公開の鍵を引く区画)— checkout の path に依らない綴り。
# 宣言が無い時は checkout の basename に畳まれるので値は同じ(dotfiles agentcli land.partition_name の
# ② が読む・③ が既定)。herdr-hud / dotfiles と同じく明示する(段 9 lane 9m 便 2d・agora-redesign #71)。
name = "doeff"

[gate]
# full = 日次検証便(ai land verify)と full_paths 便だけが使う(mode = "focus" の間、
# 窓末では走らない)。
# UV_CACHE_DIR lane 分離(operator 発注 route-c4ce3179b0 2026-08-17・方向宣言
# uv-cache-lock-convoy-root-fix): doeff の窓は自 worktree の .venv を作るため
# uv sync が構造的に要る(永続 venv 直呼びへは置換できない側)。素の global
# cache(~/.cache/uv)は艦隊の uvx 並走と同じ lock に並び、飽和帯で make sync
# だけが 23 分(L29〜L32 の 4 連敗の実測)。窓の系統専用 lane に分けると初回だけ
# cache が冷えるが、以後は warm のまま艦隊の行列に並ばない。
# 実行面 = zeus(k3s クラスタ機)— operator 裁定伝言 2026-08-17『テストはk3sでやって』
# (方向宣言 land-verify-tests-on-k3s・親方針 = fleet-compute-placement)。下の頭注の
# 「恒久解 = zeus/k3s 移設」の執行 — 前提だった semgrep 機体依存根治は 61ca71b6 で
# 着地済み。remote_check が tree を送り逐語実行する(遠隔に判断層なし・不達は
# fail-closed・--fallback-local でこの機体へ可視に倒す — ran= の 1 行が log に残る)。
# 実測 2026-08-17: zeus 全量 477 秒 緑 / Mac 実測 17 分 + 飽和帯 load 100 超。
# run-timeout 1800 = 実測の約 4 倍(timeout_s 7200 は fallback の local 実走まで収まる)。
# 内側の UV_CACHE_DIR lane(上の convoy 根治)はそのまま重ねる — \$HOME は Mac 側で
# 展開させず、実行機(zeus / fallback 時はこの機体)の sh が解く。
# ⚠ pytest は --no-sync(2026-08-17 zeus 実測): 素の uv run は暗黙の再 sync で
# make sync の maturin build(--features invariant-checks)を素の wheel(検査なし)で
# 上書きし得る — 冷えた cache lane で顕在化し test_vm_built_with_invariant_checks が
# 赤(1317 passed / 1 failed)。make sync 直後の env が権威なので再 sync を止める
# (同 tree で make sync → uv run --no-sync が緑を実測)。Mac 側も同じ潜在欠陥 —
# 従来の緑は cache 状態依存だった。
full = "python3 $HOME/dotfiles/agentcli/src/agentcli/remote_check.py --node zeus --tree . --label doeff-gate-full --sync-timeout 900 --run-timeout 1800 --fallback-local -- sh -c \"UV_CACHE_DIR=\\$HOME/.cache/uv-lanes/land-doeff make sync && UV_CACHE_DIR=\\$HOME/.cache/uv-lanes/land-doeff PYTHONUNBUFFERED=1 uv run --no-sync pytest -q -m 'not e2e and not awaiting_api_migration'\""
# 門の走行の持ち時間 2700→7200(2026-08-17)。実測 = L32 が 2713.4s で時間切れ
# (4 連敗・Mac 飽和帯)。ADR-DOE-ENFORCE-001 R6 の第 3 の締切の宣言先行便。
timeout_s = 7200

# R-7a95702d focus mode(operator 直接指示 2026-08-17「merge queue は 10 分以内・
# 2700 秒の門は許容範囲外」— 先例 = herdr-hud D568 出荷 B / ACP 同日切替)。
# operator 裁定 2026-08-17 逐語「mergeにテストは必須じゃない」: 着地の門は merge の
# 力学(rebase 成立・patch-id 突合・受入検定)のみ。テストは全部事後 =
# ① 日次全量検査(gate.full を ai land verify が使う — launchd 便は別途敷設)
# ② 全走が緑の断面だけが stable ブランチ(policy.stable_branch)へ昇格
# ③ 赤の復旧は fix-forward(容疑名指しの修理便の自動起票)— revert 自動化は不採。
# 実測根拠: Mac 飽和帯で make sync だけが 23 分・全量が門 2700s に収まらず同一便が
# 4 連敗(L29〜L32)。恒久解 = 全量検査の zeus/k3s 移設(remote_check 経由・
# 2026-08-17 実測 = zeus 全量 36s〔-n 8〕。semgrep 執行層 69 検体の機体依存根治が
# 前提 — route-doeff-semgrep-zeus-181256 起票済み)。
mode = "focus"
# focus の恒常部は宣言しない(doeff に横断走査 script なし — 門は力学のみ)。
# 方策宣言そのものへの変更だけは常時 full battery(自己参照の fail-closed)
# ⚠ 2026-08-17 operator 逐語「全量検査を無効にしてくれ」: full_paths を空に —
# 着地の窓はいかなる path でも全量検査(gate.full)を走らせない。gate.full の
# 唯一の消費者 = 日次検証便(ai land verify・launchd)。方策 toml の変更も
# 窓では力学のみで着地する(検分は事後 + fix-forward)。復活は operator 裁定で。
full_paths = []

# R-94ede4bc 投機列車(operator 直接指示 2026-08-17「merge queue の投機テストを
# 全 repo で適用」— 連邦の機構 = dotfiles land.py・先例 = dotfiles 裁定 2026-08-16
# decision-land-speculation-enable / herdr-hud D568)。窓は確定 train(消費済み
# 緑の先端・公開対象)と投機 tip を分け、gate の実行だけを backend へ並列に
# 発注する(判定の消費は直列同値・赤 1 便で窓ごと abort する形の根治)。厳密 bool。
# 戻し方 = この行を外す便を着地させる(既定 off・law opt-in-inert)。
speculation = true
# 検査 backend(law policy-in-toml)。mode = "focus" で gate 実体が軽くなったため
# local のままで足りる。恒久解 = k3s self-hosted runner(external backend 段 2)。
[gate.backend]
kind = "local"
max_parallel = 1
join_s = 60

[gate.tiers]
# ⚠ mode = "focus" の間、この表は着地の解決に使われない(mode 未宣言への戻しだけが
# 読む — herdr-hud / ACP と同じ移行残置。撤去は日次 verify 連続緑の再訪条件で別便)。
# lane 分離は [gate].full の註と同じ(convoy 根治)。
"docs/adr/" = "UV_CACHE_DIR=$HOME/.cache/uv-lanes/land-doeff make sync && UV_CACHE_DIR=$HOME/.cache/uv-lanes/land-doeff PYTHONUNBUFFERED=1 uv run pytest -q docs/adr"
"docs/" = "true"
"notes/" = "true"
"README.md" = "true"
"CHANGELOG.md" = "true"
".agents/land-queue.toml" = "true"

[policy]
# ⚠ 名簿は意味名で書く(dotfiles ADR-DOTFILES-017 R-6f25e8f4)。"machine" は種別の併記(互換)。
# "land-runner@agora-1" = pod の窓の走行体(下の runner = "pod" と対の二重宣言 — 名簿に無いと pod の窓は開かない)。
run_executors = ["landrun-doeff", "landrun-rollout", "machine", "land-runner@agora-1"]
# 着地の窓を回す機体(dotfiles ADR-DOTFILES-017 law land-runner-by-repo-owner・段 9 lane 9m 便 2d・
# agora-redesign #71・operator 裁定 2026-09-14 案 A)。この repo は個人の repo(会社 repo ではない)
# なので、pod の service(herdr-hud deploy の container land-api・実行者 land-runner@agora-1)が
# 窓を回す。Mac 側の起こし口はこの宣言を**本線の先端から**読んで「この repo の窓は pod が回す」
# と名乗って見送る。戻すには runner の行を消す(Mac の窓が再び回る・1 行の便)。押す鍵 =
# Secret land-key-doeff(GitHub の deploy key `agora-1-pod (write) land-runner doeff`・herdr-hud
# deploy の env AGORA_LAND_SSH_KEYS の doeff の項 — 鍵は repo ごとに 1 つ・#71 便 2b)。
# ⚠ `auto_run = true` は残す(pod の窓と `--machine` の資格の宣言 = 種別の照合)。
runner = "pod"
auto_run = true
urgent_slots = 1
push = "origin"
base_move_follow_retries = 2
# 日次検証の全走が緑の断面だけが昇格する(R-7a95702d)
stable_branch = "stable"
# 日次の全体検証(ai land verify)が赤になった時の報せの宛先(ADR-DOTFILES-017 R-55ee0b39 ②・card acp:kanban-issue:ki-03598df36cce)。
# **会話 id ちょうど** — 席名・pane・作業セッション名に結ばない(撤収すると解けない)。承認の名簿 policy.reviewers は
#   「承認してよい資格」であって宛先ではない(別の問い)ので、後退先にも使わない。
# 宛先の会話 = c-D6AFCPB1VRMNTVN9ECSZMCAS3T = agora-redesign #467(日次の赤が見えない)の根の配り手で、この repo の日次を
#   母集団に含める card ki-03598df36cce を開いて計画の依頼を出した会話。ACP の会話の行の status.turnRoute が agent-job
#   なので ACP の郵便の経路が在る(pane の会話には経路が無い — herdr-hud の宣言 c-01M278C… がその形)。
#   選んだ根拠と代案 = card ki-03598df36cce のスレッド(計画段の裁定 2026-09-19)。
# ⚠ 既知の滞留(R-55ee0b39 ⑧・card ki-9655dc6151b3): 差出人が land-queue(operator でも会話でもない)なので ACP の配達の
#   判定は inReplyTo の無いこの報せを held にする。⇒ この宣言で台帳の結末は no-addressee(誰も呼ばれていない)から
#   posted → 次の走の読み直しで refused(held: sender-is-not-operator-or-conversation)に変わる。台帳が「届かなかった」を
#   正しく名乗る形が先で、会話の手番に載せるのは ACP 側の別の決定。
# 戻し方 = この 1 行を消す(two-way door・決定の記録 = card ki-03598df36cce のスレッド 2026-09-19)。
verify_notice_to = ["c-D6AFCPB1VRMNTVN9ECSZMCAS3T"]

# ───────────────────────────────────────────────────────────────────────────
# 全体・広範囲テストの走行の受付(dotfiles ADR-DOTFILES-027 law
# broad-test-runs-need-an-explicit-grant・operator 指示 2026-09-10「全体テストは
# 明示の指示と警告なしに撃てない形にする」)の**値の宣言**。
# 2026-09-15・段 11 lane 11k(agora-redesign #64 の c)で、この repo も受付に参加した。
#
# ⚠ 判定(広範囲か)・30 分の窓の合算・許可の消費は**正本 1 点**(agora の道具立て
#   ~/dotfiles/agent/tests/broad_run_admission.py)が持つ。この repo は値の宣言と
#   conftest.py の委譲 1 行だけを持ち、判定の写しを持たない。
# ⚠ 道具立ての無い機体(この repo だけを clone した利用者)では conftest が素通しにして
#   1 行を名乗る — 許可の口も頭脳も無い宿で門を立てると検体が 1 本も走らない。
#
# 値の根拠(2026-09-15 の実測 = 収集だけの走行 `pytest -q -m 'not e2e' --collect-only`):
#   1,521 本(e2e を除く)。dir 単位の最大は tests/effects の 219 本、次が tests/core 186・
#   docs/adr 153・tests/cli 97・tests/public_api 96。file 単位の最大は tests/test_scheduler.py の 75 本。
#   ⇒ 1 走行 400 = 触った層(dir 1〜2 つ)の検証は焦点走のまま通し、全数(1,521)は広範囲。
#      窓 800 = 分けて撃っても 30 分で全数に届く前に当たる。
#   ⚠ 数字は日付と断面に固定した観測で、守るのは「1 走行の上限 < 全数」の関係のほう。
[test-admission]
per_run_max = 400
window_max = 800
window_s = 1800
# 日次の全体検証(ai land verify)が [gate] full で撃つ pytest の走行の数 = 1 本
# (remote_check の中の `uv run --no-sync pytest -q -m 'not e2e'`。遠隔が使えない拍は
#  同じ 1 本をこの機体で走らせる — どちらでも走行は 1 つ)。
full_sessions = 1
