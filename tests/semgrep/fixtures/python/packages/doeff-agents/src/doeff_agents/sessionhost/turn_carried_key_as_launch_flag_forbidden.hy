;;; Hit fixture: 手番の荷(正本が ACP の行に在る欄)を旗にすると、行が第 2 の正本になる —
;;; card acp:kanban-issue:ki-a068efe8f6d9 / 法 ACP 575b1e。継続の腕は行を読み足すのではなく、
;;; 送りの params で手番ごとに名乗り直す。

(setv LAUNCH-FLAG-KEYS #("auto_compact_window" "memory_dir"))
